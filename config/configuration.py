#Load appropriate credentials in the config list
#used to access database in main file ('cnx')

config = {
  'user': 'user',
  'password': 'password',
  'host': 'host',
  'database': 'database',
  'raise_on_warnings': True,
}
